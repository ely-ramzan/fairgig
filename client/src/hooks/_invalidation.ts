/**
 * TanStack Query invalidation map (documentation).
 *
 * - createShift → shifts, workerSummary, workerTrends
 * - importCsv → shifts, importHistory
 * - uploadScreenshot → shifts
 * - verify shift → verification-queue, shifts
 * - grievance mutations → grievances
 */

export {};
