#!/usr/bin/env node
/**
 * Placeholder — Phase 8 adds real gzip budget checks against dist/assets.
 * Exit 0 so `npm run budget` does not fail CI before bundles exist.
 */
console.log('Bundle budget check: skipped (no dist or Phase 8 tooling).');
process.exit(0);
